---
# PAN-OS modules run on the control node and talk to the firewall over its API,
# so we use a local connection; the real target comes from panos_provider.
all:
  hosts:
    panos_fw:
      ansible_connection: local
      ansible_python_interpreter: "{{ ansible_playbook_python }}"
